query GetLanguages {
    languages {
        name
        optionsSchema
    }
}

mutation GetOutputCode($skema: String!, $options: Json!) {
    output: getOutput(skema: $skema, options: $options) {
        code
    }
}